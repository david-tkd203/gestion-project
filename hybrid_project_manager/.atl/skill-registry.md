# Skill Registry — Hybrid PM (VINCULOsync)

Generated by `sdd-init` on 2026-07-02.
Scans user-level skill directories (`~/.config/opencode/skills/`, `~/.agents/skills/`, and mirrors).
No project-level skills found under the project root.

Skipped: `sdd-*`, `_shared`, `skill-registry`, `template-skill`, `.system`, composio automation skills (600+ identical Rube MCP wrappers — load on demand via `skill()` tool).

---

## Git & PR Workflow

### branch-pr
**Path**: `~/.config/opencode/skills/branch-pr/SKILL.md`
**Trigger**: creating, opening, or preparing PRs for review.
**Compact Rules**:
- Every PR MUST link an approved issue (`Closes #N` / `Fixes #N` / `Resolves #N`).
- Every PR MUST have exactly one `type:*` label.
- Branch names: `^(feat|fix|chore|docs|style|refactor|perf|test|build|ci|revert)\/[a-z0-9._-]+$`.
- Conventional commits only: `type(scope): description`.
- No `Co-Authored-By` or AI attribution trailers.
- Automated checks must pass before merge.

### chained-pr
**Path**: `~/.config/opencode/skills/chained-pr/SKILL.md`
**Trigger**: PRs over 400 lines, stacked PRs, review slices.
**Compact Rules**:
- Split PRs over 400 changed lines unless maintainer accepts `size:exception`.
- Each PR reviewable in ~60 minutes; one deliverable work unit per PR.
- Keep tests/docs with the unit they verify.
- State start, end, prior dependencies, follow-up, and out-of-scope in every chained PR.
- Every child PR includes a dependency diagram marking the current PR with `📍`.
- Do not mix chain strategies after user chooses one.

### work-unit-commits
**Path**: `~/.config/opencode/skills/work-unit-commits/SKILL.md`
**Trigger**: implementation, commit splitting, chained PRs, keeping tests/docs with code.
**Compact Rules**:
- Commit by deliverable work unit, not by file type.
- Keep tests in the same commit as the behavior they verify.
- Keep docs with the user-visible change they explain.
- Each commit should be a candidate chained PR when the change grows.
- If SDD tasks forecast >400-line change, group commits into chained PR slices before implementation.

### issue-creation
**Path**: `~/.config/opencode/skills/issue-creation/SKILL.md`
**Trigger**: creating GitHub issues, bug reports, or feature requests.
**Compact Rules**:
- Blank issues disabled — MUST use a template (bug report or feature request).
- Every issue gets `status:needs-review` on creation.
- Maintainer MUST add `status:approved` before any PR can be opened.
- Questions go to Discussions, not issues.

---

## Code Quality & Review

### simplify
**Path**: `~/.config/opencode/skills/simplify/SKILL.md`
**Trigger**: readability, maintainability, complexity reduction after behavior is understood.
**Compact Rules**:
- Preserve behavior exactly — all inputs, outputs, side effects, error behavior must remain identical.
- Follow project conventions; match neighboring code style.
- Prefer clarity over cleverness; explicit > compact when compact requires a mental pause.
- Don't inline away meaningful names; don't merge unrelated logic.
- Scope to recently modified code; avoid drive-by refactors.
- Separate refactoring from feature work.

### judgment-day
**Path**: `~/.config/opencode/skills/judgment-day/SKILL.md`
**Trigger**: judgment day, dual review, adversarial review, juzgar.
**Compact Rules**:
- Launch two blind judges in parallel; never review code yourself.
- Wait for both judges before synthesis; never accept partial verdict.
- Classify WARNING as real only if normal intended use can trigger it.
- Ask before fixing Round 1 confirmed issues.
- Re-judge in parallel after any fix.
- Terminal states: `JUDGMENT: APPROVED` or `JUDGMENT: ESCALATED`.
- After 2 fix iterations with remaining issues, ask user whether to continue.

---

## Documentation

### cognitive-doc-design
**Path**: `~/.config/opencode/skills/cognitive-doc-design/SKILL.md`
**Trigger**: writing guides, READMEs, RFCs, onboarding, architecture, or review-facing docs.
**Compact Rules**:
- Lead with the answer; context comes after.
- Progressive disclosure: happy path first, then details, edge cases, references.
- Use tables, checklists, examples over prose that must be remembered.
- For PR docs: state what to review first, what's out of scope, link prev/next PR.

### comment-writer
**Path**: `~/.config/opencode/skills/comment-writer/SKILL.md`
**Trigger**: PR feedback, issue replies, reviews, Slack messages, or GitHub comments.
**Compact Rules**:
- Be useful fast — start with the actionable point, don't recap the whole PR.
- Keep it short: 1-3 paragraphs or tight bullet list.
- Explain why when asking for a change.
- Match thread language (Rioplatense Spanish/voseo if Spanish).
- No em dashes — use commas, periods, or parentheses.

---

## Frontend & Design

### design-systems
**Path**: `~/.config/opencode/skills/design-systems/SKILL.md`
**Trigger**: ANY frontend work — design, landing page, UI, component, hero, layout, visual output.
**Compact Rules**:
- Pick a design archetype before writing code (EDITORIAL, CINEMATIC, TERMINAL, ORGANIC, BRUTALIST, LUXURY, HAND-DRAWN, CYBERPUNK, COSMIC, CORPORATE).
- ANTI-MEDIOCRITY: no Inter/Space Grotesk primary font, no purple/violet gradient on white, no centered hero + two buttons, no 3-column card grid as main pattern.
- Colors: 1 background (offset from pure white/black), 1 foreground, 1 bold accent (never default violet/blue), 1 muted.
- Typography: display font with personality, fluid scale with `clamp()`, never pixel values.
- Pick ONE signature element per page.
- Brand tokens available at `~/.agents/sleek/public/designs/*.json` (55+ brands).

---

## Testing

### webapp-testing
**Path**: `~/.agents/skills/webapp-testing/SKILL.md`
**Trigger**: testing local web applications, verifying frontend functionality, UI debugging, screenshots.
**Compact Rules**:
- Write native Python Playwright scripts; use `scripts/with_server.py` for server lifecycle.
- Always run `--help` first on helper scripts; treat them as black boxes.
- Use `sync_playwright()` for synchronous scripts; always close browser.
- Wait for `page.wait_for_load_state('networkidle')` before DOM inspection on dynamic apps.
- Reconnaissance-then-action: navigate, screenshot/inspect, identify selectors, execute actions.
- Multiple servers supported: `--server "cmd" --port N` repeated.

### go-testing
**Path**: `~/.config/opencode/skills/go-testing/SKILL.md`
**Trigger**: Go tests, go test coverage, Bubbletea teatest, golden files.
**Compact Rules**:
- Prefer table-driven tests with `t.Run(tt.name, ...)`.
- Test behavior and state transitions, not implementation trivia.
- Use `t.TempDir()` for filesystem tests; never rely on real home directory.
- Keep integration tests skippable with `testing.Short()`.
- Golden files must be deterministic; update only through repo's `-update` path.
- NOTE: Not applicable to this project (no Go code) — retained for cross-project consistency.

---

## Codebase Exploration

### codemap
**Path**: `~/.config/opencode/skills/codemap/SKILL.md`
**Trigger**: codebase documentation, initial repository mapping, unfamiliar codebase.
**Compact Rules**:
- Check for `.slim/codemap.json` before re-initializing.
- Include core code/config files only; exclude tests, docs, build artifacts.
- Delegate codemap writing to fixer agents — one per folder.
- Detect changes with `codemap.mjs changes`; only update affected codemaps.
- Register codemap in AGENTS.md with a `## Repository Map` section.

### clonedeps
**Path**: `~/.config/opencode/skills/clonedeps/SKILL.md`
**Trigger**: clone dependencies, inspect dependency/source internals, understand SDK/framework behavior from source.
**Compact Rules**:
- Check `.slim/clonedeps.json` before re-planning.
- Delegate discovery to `@librarian`; prefer 0-3 strong recommendations.
- Orchestrator owns final approval; verify refs with `git ls-remote`.
- Clone into `.slim/clonedeps/repos/<safe-name>/`; don't run install/build/test from clones.
- Write `.slim/clonedeps.json` manifest; update `.gitignore` and `.ignore` with marker blocks.
- Register in AGENTS.md under `## Cloned Dependency Source`.

---

## Skill Authoring

### skill-creator
**Path**: `~/.config/opencode/skills/skill-creator/SKILL.md`
**Trigger**: new skills, agent instructions, documenting AI usage patterns.
**Compact Rules**:
- A skill is a runtime instruction contract for an LLM, not human documentation.
- `description` MUST be one physical line, quoted, YAML-safe, trigger words first, <=250 chars.
- Frontmatter MUST include `name`, `description`, `license`, `metadata.author`, `metadata.version`.
- Sections in order: Activation Contract, Hard Rules, Decision Gates, Execution Steps, Output Contract, References.
- Target 180-450 body tokens; move examples/schemas/edge cases to `assets/` or `references/`.
- No `Keywords` section; preserve trigger words in `description`.

---

## Document Skills (available via `skill()` tool)

| Skill | Path | Use |
|-------|------|-----|
| docx | `~/.agents/skills/document-skills/docx/` | Create/edit/analyze .docx files |
| pdf | `~/.agents/skills/document-skills/pdf/` | PDF manipulation, form filling, text extraction |
| xlsx | `~/.agents/skills/document-skills/xlsx/` | Spreadsheet creation, formulas, data analysis |
| pptx | `~/.agents/skills/document-skills/pptx/` | Presentation creation and editing |

---

## Other Available Skills (load via `skill()` tool on demand)

| Skill | Trigger |
|-------|---------|
| agent-browser | Browser automation CLI for AI agents |
| artifacts-builder | Multi-component Codex.ai HTML artifacts (React, Tailwind, shadcn/ui) |
| brand-guidelines | Anthropic brand colors and typography |
| canvas-design | Visual art in .png and .pdf |
| changelog-generator | User-facing changelogs from git commits |
| mcp-builder | Create high-quality MCP servers |
| file-organizer | Intelligent file/folder organization |
| image-enhancer | Improve screenshot quality |
| internal-comms | Status reports, updates, FAQs, incident reports |
| invoice-organizer | Invoice/receipt organization for tax prep |
| langsmith-fetch | Debug LangChain/LangGraph agents via traces |
| theme-factory | Style artifacts with 10 pre-set themes |
| content-research-writer | Research-backed content with citations |
| competitive-ads-extractor | Extract competitor ads from ad libraries |
| domain-name-brainstormer | Domain name ideas + availability check |
| lead-research-assistant | Identify high-quality leads |
| meeting-insights-analyzer | Analyze meeting transcripts for behavioral patterns |
| twitter-algorithm-optimizer | Optimize tweets for Twitter's algorithm |
| tailored-resume-generator | Tailored resumes from job descriptions |
| developer-growth-analysis | Analyze coding patterns, curate learning resources |
| raffle-winner-picker | Random winner selection from lists |
| youtube-downloader | Download YouTube videos |
| slack-gif-creator | Animated GIFs optimized for Slack |
| skill-share | Create and share skills on Slack |
| connect / connect-apps | Connect to external apps (Gmail, Slack, GitHub, etc.) |

---

## Composio Automation Skills

600+ automation skills available at `~/.agents/skills/composio-skills/` for integrating with external services (Gmail, Slack, GitHub, Notion, Jira, Salesforce, etc.) via Rube MCP. Load individual skills via the `skill()` tool when a specific integration is needed. Not pre-loaded to keep the registry compact.
